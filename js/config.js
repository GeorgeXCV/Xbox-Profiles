module.exports.config = {
    apiKeyOpenXBL: "YOUR_API_KEY", // https://xbl.io/ (Better API, higher rate limits)
    apiKeyXboxAPI: "YOUR_API_KEY" // https://xapi.us/ (Low rate limits, only use when have to)
}